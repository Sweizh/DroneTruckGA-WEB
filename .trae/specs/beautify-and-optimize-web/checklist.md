# Checklist

## 性能
- [x] 遗传算法在 Web Worker 中执行，主线程在 GA 运行期间保持 ≥30fps 响应
- [x] 点击"停止"后 ≤500ms 内主线程恢复"就绪"态
- [x] GA 运行期地图/曲线渲染已节流（每 N 代 + rAF 合批），不再每代全量重绘
- [x] 收敛曲线数据点 >500 时已降采样，长序列下无卡顿
- [x] `FitnessFunction` 热点已优化（Map 查找替代 findIndex、避免冗余 sqrt）

## 多终端适配
- [x] 桌面端（≥1200px）布局与原版一致或更优
- [x] 平板端（768–1199px）右侧控制台以可折叠抽屉形式可展开/收起
- [x] 手机端（<768px）导航为底部 Tab Bar，顶栏图标化，统计卡片 2 列，表单单列
- [x] 地图支持双指 pinch-zoom 与单指拖拽，且不触发页面整体滚动
- [x] 移动端按钮点击热区 ≥40×40px
- [x] Canvas 尺寸随容器 ResizeObserver 自适应，DPR 处理正确（无 transform 累积）

## 视觉美化
- [x] 设计令牌（间距/圆角/阴影/动效曲线）已统一并替换硬编码值
- [x] 浅色主题下所有组件（map-legend / map-ctrl / topbar 等）视觉与深色主题对齐
- [x] 运行态有进度条流光、状态灯呼吸、按钮微动效
- [x] 地图与曲线在无数据时展示引导文案与图标
- [x] 收敛曲线含最优值参考线、单位标注、图例
- [x] 表格粘性表头与移动端字号优化生效

## GitHub Pages 交付
- [x] `.github/workflows/deploy.yml` 存在且 push 到 main 可触发部署
- [x] 所有资源路径为相对路径，在 `/<repo>/` 子路径下加载正常
- [x] `README.md` 包含本地预览与部署说明
- [x] 本地静态服务器冒烟测试通过：C101 加载、GA 运行、页面切换、地图缩放、移动端断点均正常

## 兼容与回归
- [x] 原有功能（Solomon 算例加载、配置导入/导出、主题切换、客户/仓库增删）全部保留可用
- [x] 无控制台报错；Worker 错误有降级提示
